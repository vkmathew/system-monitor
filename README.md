# Mini System Resource Monitor

A simple Python script that prints CPU, memory, and disk usage every few seconds using the `psutil` library.

## Usage

Install dependency:
pip install psutil

Run the script:
python system_monitor.py

Press `Ctrl + C` to stop.

